export type TURLData = {
    url: string
    label: string
}

export type TQueryData = {
    url: string
    unique_key: string,
    query: string, 
}

export type TDatabaseData = {
    unique_key: string,
    question: string,
    answer: string,
    totalResponseTime: string
}